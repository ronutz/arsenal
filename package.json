{
  "//": "================================================================",
  "//1": "ARSENAL: the ronutz.com application. This repo IS the deployable",
  "//2": "site: a privacy-first, local-compute network/security toolbox PWA.",
  "//3": "It is SELF-CONTAINED -- the tool engines live in-house under",
  "//4": "src/lib/ and there is no runtime dependency on any external engine.",
  "//5": "Code is Apache-2.0; content (Learn articles and copy) is CC BY 4.0.",
  "//6": "================================================================",
  "name": "@ronutz/arsenal",
  "version": "0.1.0",
  "description": "The ronutz.com application: a privacy-first, local-compute network/security toolbox PWA (Next.js).",
  "license": "Apache-2.0",
  "author": "Rodolfo Nützmann (https://ronutz.com)",
  "homepage": "https://ronutz.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ronutz/arsenal.git"
  },
  "type": "module",
  "scripts": {
    "//build-note": "build runs next build THEN pagefind. Pagefind indexes the static HTML in out/ to produce the client-side search index. The two steps must run in order.",
    "dev": "next dev",
    "build:next": "next build",
    "build:search": "pagefind --site out",
    "build": "next build && pagefind --site out && tsx scripts/gen-machine-legible.mts",
    "start": "next start",
    "lint": "next lint",
    "prebuild": "node scripts/check-tool-articles.mjs && node scripts/check-changelog.mjs && node scripts/check-tools-registry.mjs && node scripts/check-vendor-namespace.mjs && node scripts/check-tool-docs.mjs && node scripts/check-user-guide.mjs && node scripts/check-icu-messages.mjs && node scripts/check-css-coverage.mjs && node scripts/copy-locales.mjs && tsx scripts/build-openapi.mts && node scripts/gen-api-endpoints.mjs && node scripts/gen-locale-coverage.mjs && node scripts/gen-build-info.mjs",
    "update-swagger-ui": "node scripts/update-swagger-ui.mjs"
  },
  "//7": "Self-contained: tool engines are in-house under src/lib/; no external engine dependency.",
  "//overrides": "postcss pinned to >=8.5.10 to clear GHSA-qx2v-qp2m-jg93 (XSS in CSS stringify); build-time tooling only.",
  "overrides": {
    "postcss": "^8.5.10"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^5.2.1",
    "next": "^15.5.19",
    "next-intl": "^3.26.5",
    "next-mdx-remote": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "pagefind": "^1.5.2",
    "tsx": "^4.23.0",
    "typescript": "^5.7.0"
  }
}
