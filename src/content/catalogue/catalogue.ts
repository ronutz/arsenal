// ============================================================================
// src/content/catalogue/catalogue.ts
// ----------------------------------------------------------------------------
// THE CONSOLIDATED TOOL CATALOGUE — internal source of truth.
//
// This encodes the full ARSENAL tool inventory and its governance disposition:
// what is LIVE, what is QUEUED to build, what is DEFERRED (behind the reserved
// gating model), and what is DROPPED (control-defeat / frame-forging, refused on
// principle). It reflects the ratified consolidation (merges M1/M2/M4/M5; M3/M6/
// M7 kept separate), the two PRIME INCLUDE rulings (f5-bigip-persistence-cookie
// ENCODE; asm-waf-inspector deep/fingerprint/diff/cohort), F5-first priority, and
// the UDP decoder additions.
//
// This module is data only — it is rendered by the (unlisted) admin console. It
// is intentionally NOT wired into the public tool registry (src/config/tools.ts),
// the sitemap, or search.
// ============================================================================

export type CatalogueStatus = "live" | "queued" | "deferred" | "dropped";

/** PKG-002 disposition. */
export type Disposition = "built" | "accept" | "defer" | "drop";

export interface CatalogueEntry {
  slug: string;
  name: string;
  family: string;
  status: CatalogueStatus;
  /** Human-readable posture, e.g. "decode + encode", "decode-only", "analyze". */
  posture: string;
  disposition: Disposition;
  /** RFC / IANA / standards anchors. */
  specs?: string[];
  /** Golden vectors: a count where known, or true for "present". */
  vectors?: number | true;
  /** F5-priority (moves to the front of the build queue). */
  f5?: boolean;
  /**
   * Global build-priority rank (1 = build first), PRIME-ratified 2026-06-28.
   * Spans every accepted tool across all families: a single highest-to-lowest
   * value ordering, the durable source of truth for the Phase-1 build queue.
   * Present on accepted (not-yet-built) tools; absent on live/deferred/dropped.
   */
  rank?: number;
  /** Fortinet-platform tool. */
  fortinet?: boolean;
  /** Netskope-platform tool. */
  netskope?: boolean;
  /** Extreme Networks-platform tool. */
  extreme?: boolean;
  /** Marks a brand-new addition this revision (UDP set). */
  isNew?: boolean;
  /** LIVE tool that still needs consolidation work (a ratified merge is unbuilt). */
  consolidationPending?: boolean;
  /** Consolidation / merge / priority note. */
  note?: string;
}

/** Family display order. Must list EVERY family string used by an entry below,
    or that family's tools silently drop out of the grouped admin roadmap view
    (which maps over this array). Keep in sync when adding a new family. */
export const FAMILIES: string[] = [
  "Security & WAF",
  "F5 LTM, iRules & platform",
  "F5 GTM, AFM & APM",
  "F5 automation (AS3 / DO)",
  "F5 Distributed Cloud (XC)",
  "F5OS platform (VELOS / rSeries)",
  "Fortinet FortiGate & FortiOS",
  "Fortinet security, VPN & SD-WAN",
  "Fortinet ecosystem & identity",
  "Netskope SSE / SASE",
  "Extreme switching & fabric (EXOS / VOSS / SPB)",
  "Extreme NAC & cloud (ExtremeControl / Platform ONE)",
  "Identity & tokens",
  "PKI",
  "Protocol & packet decoders",
  "Networking & addressing",
  "HTTP & web",
  "Web front-end & CSS",
  "TLS & transport",
  "Logging & SIEM formats",
  "Encoding",
  "Hashing",
  "Identifiers & time",
  "Text & utilities",
  "Python",
  "Cloud-native & containers",
  "Infrastructure as code (Terraform / Ansible)",
];

export const CATALOGUE: CatalogueEntry[] = [
  // ---- LIVE (10) ---------------------------------------------------------
  { slug: "jwt", name: "JWT decoder", family: "Identity & tokens", status: "live", posture: "decode / verify", disposition: "built", specs: ["RFC 7519", "RFC 7515", "RFC 7518"], vectors: true },
  { slug: "pkce", name: "PKCE helper", family: "Identity & tokens", status: "live", posture: "generate / verify", disposition: "built", specs: ["RFC 7636", "RFC 6749"], vectors: true },
  { slug: "x509", name: "X.509 inspector", family: "PKI", status: "live", posture: "decode / explain", disposition: "built", specs: ["RFC 5280", "RFC 6960", "RFC 7633"], vectors: 7, note: "Decodes structure, validity, key, and v3 extensions; now also extracts the certificate's revocation configuration (CRL distribution points, AIA OCSP + CA Issuers, Must-Staple). Reads the pointers only, never contacts them (zero egress)." },
  { slug: "cipher", name: "Cipher-suite decoder", family: "TLS & transport", status: "live", posture: "decode / rate", disposition: "built", specs: ["IANA TLS Cipher Suites", "RFC 8446", "RFC 5246", "RFC 8447", "RFC 7465"], vectors: 15 },
  { slug: "ipv6", name: "IPv6 toolkit", family: "Networking & addressing", status: "live", posture: "expand / compress / explain", disposition: "built", specs: ["RFC 4291", "RFC 5952"], vectors: true },
  { slug: "cidr", name: "CIDR / subnetting", family: "Networking & addressing", status: "live", posture: "calculate / explain", disposition: "built", specs: ["RFC 4632"], vectors: true, consolidationPending: false, note: "M1 done: single-subnet plus VLSM, supernet/aggregate, and overlap/gap detection all shipped (RFC 4632)." },
  { slug: "base64", name: "Base64 / Base32 / Hex / Percent codec", family: "Encoding", status: "live", posture: "encode / decode", disposition: "built", specs: ["RFC 4648", "RFC 3986"], vectors: true, consolidationPending: false, note: "M5 done: unified codec over Base64, Base64URL, Base32, Base16/hex, and percent-encoding (RFC 4648 sections 4-8, RFC 3986)." },
  { slug: "hash", name: "Hash", family: "Hashing", status: "live", posture: "compute", disposition: "built", specs: ["FIPS 180-4", "FIPS 202"], vectors: true },
  { slug: "hmac", name: "HMAC", family: "Hashing", status: "live", posture: "compute / verify", disposition: "built", specs: ["RFC 2104", "FIPS 198-1"], vectors: true },
  { slug: "hash-preimage-finder", name: "Hash preimage finder", family: "Hashing", status: "live", posture: "demonstrate / crack (bounded)", disposition: "built", vectors: 22, specs: ["RFC 1321", "RFC 3174", "FIPS 180-4"], isNew: true, note: "Paste an MD5, SHA-1, or SHA-256 hash, choose an alphabet and length, and watch a bounded local brute-force search either recover a weak input in milliseconds or run out of keyspace on anything with real entropy. No dictionary, no wordlist, no precomputed table (unlike lookup services such as CrackStation): pure local enumeration and hashing, capped so it only ever recovers trivially weak inputs. A teaching tool for why fast, unsalted hashes fail, pairing every result with the defenses (salting, slow KDFs, algorithm choice). Browser-only by design (not on the API, D-72); control-defeat capabilities are excluded (D-53). MD5/SHA-1/SHA-256 are verified against published test vectors." },
  { slug: "uuid", name: "UUID", family: "Identifiers & time", status: "live", posture: "generate / parse", disposition: "built", specs: ["RFC 9562"], vectors: true },

  // ---- QUEUED — Security & WAF (F5-first) ---------------------------------
  { slug: "f5-bigip-persistence-cookie", name: "BIG-IP persistence cookie", family: "Security & WAF", status: "live", posture: "decode + encode", disposition: "built", specs: ["F5 K6917", "F5 K7784", "RFC 1918", "RFC 5952"], vectors: 23, f5: true, note: "Decodes all four unencrypted formats (default IPv4, IPv4/IPv6 route domain, IPv6) with their differing byte orders and detects the AES-encrypted form; also encodes an address+port back into a cookie (PRIME decode+encode ruling). Flags internal-address disclosure and points to cookie encryption (K7784)." },
  { slug: "asm-waf-inspector", name: "ASM / Advanced WAF inspector", family: "Security & WAF", status: "queued", posture: "identify / structure / explain + deep / fingerprint / diff / cohort", disposition: "accept", rank: 24, specs: ["F5 BIG-IP ASM / Advanced WAF"], f5: true, note: "Deep / fingerprint / diff / cohort now INCLUDED (PRIME): legitimately valuable for practitioners." },
  // Added 2026-07-02: the built, focused declarative-policy explainer (distinct from the broader asm-waf-inspector above, which keeps its fingerprint/diff/cohort scope).
  { slug: "f5-awaf-declarative-policy-explainer", name: "F5 Advanced WAF declarative-policy explainer", family: "Security & WAF", status: "live", posture: "explain (deterministic)", disposition: "built", vectors: 6, specs: ["F5 BIG-IP Advanced WAF declarative policy (schema v17.1)"], f5: true, isNew: true, note: "Paste a BIG-IP Advanced WAF (ASM) declarative security policy in JSON (the policy object) and get a section-by-section, plain-language reading, grounded in F5's published declarative-policy schema. Reads the values, not just echoes them: transparent enforcement means monitor-only, plus signature staging, X-Forwarded-For trust, Data Guard off, and enforced cookies missing Secure or HttpOnly. Covers about 55 policy sections across identity, enforcement, learning, traffic surface, protections and content profiles, and respects the template-delta rule (an absent section means template default, not disabled). The declarative parallel to the F5XC service-policy explainer. Decode-only; grounded in F5 clouddocs schema v17.1 (published versions v16.0-v17.5); nothing leaves the browser." },
  { slug: "csp-evaluator", name: "CSP evaluator", family: "Security & WAF", status: "queued", posture: "evaluate / grade", disposition: "accept", rank: 12, specs: ["CSP Level 3 (W3C)"], isNew: true, note: "Paste a Content-Security-Policy header and get each directive explained and the policy graded: flags unsafe-inline, unsafe-eval, wildcard and data: sources, missing object-src / base-uri / frame-ancestors, and nonce / hash gaps. The deep-CSP companion to secure-headers. Decode and grade only, offline." },
  { slug: "path-traversal-decoder", name: "Path traversal decoder", family: "Security & WAF", status: "queued", posture: "decode / normalize", disposition: "accept", rank: 23, specs: ["RFC 3986", "OWASP Path Traversal"], isNew: true, note: "Paste a path-traversal string and get it decoded and normalized: resolves ../ and ..\\ sequences, single and double percent-encoding, overlong UTF-8, and mixed separators, and shows the final resolved path plus the evasions used. A decoder for learning and log analysis, not an exploit tool; it never fetches or touches a filesystem (D-53)." },
  { slug: "ssrf-url-classifier", name: "SSRF URL classifier", family: "Security & WAF", status: "live", posture: "classify / flag", disposition: "built", vectors: 26, specs: ["RFC 1918", "RFC 3986"], isNew: true, note: "Paste a URL and get its destination classified: loopback, private (RFC 1918), link-local, cloud metadata endpoints (169.254.169.254 and IPv6/vendor equivalents), CGNAT, or reserved space, with decimal, octal, and hex IP obfuscation decoded to reveal an internal address hidden from a naive filter. Dangerous non-HTTP schemes and embedded credentials are flagged, with an SSRF risk level and defenses (allow-lists, blocking internal ranges, resolve-then-check, no redirects). A defensive classifier for learning and allow-list design; it never resolves DNS and never issues the request (D-53). Grounded in RFC 1918/3927/6598/3986 and the OWASP SSRF prevention cheat sheet." },
  { slug: "sql-injection-pattern-explainer", name: "SQL injection pattern explainer", family: "Security & WAF", status: "queued", posture: "decode / annotate", disposition: "accept", rank: 29, specs: ["OWASP SQL Injection"], isNew: true, note: "Paste a SQL injection string and get its anatomy annotated: tautologies (OR 1=1), comment terminators, stacked queries, UNION SELECT column matching, and common encodings, with the defense (parameterized queries) called out. Education-only decode and annotate of a pasted string; it never generates payloads or targets a database (D-53); to reconfirm at build." },
  { slug: "xss-payload-explainer", name: "XSS payload explainer", family: "Security & WAF", status: "queued", posture: "decode / annotate", disposition: "accept", rank: 30, specs: ["OWASP XSS"], isNew: true, note: "Paste a cross-site-scripting string and get it annotated: the injection context (HTML, attribute, JS, URL), the sink, the tag or event handler used, and the encoding or evasion, with the defense (context-aware output encoding and CSP) called out. Education-only decode and annotate of a pasted string; it never generates payloads or targets a site (D-53); to reconfirm at build." },
  { slug: "waf-evasion-normalizer", name: "WAF evasion normalizer", family: "Security & WAF", status: "queued", posture: "defensive canonicalization only", disposition: "accept", rank: 116, f5: true, note: "Normalization for analysis only; never evasion generation." },
  { slug: "secure-headers", name: "Secure headers", family: "Security & WAF", status: "live", posture: "analyze / grade", disposition: "built", specs: ["OWASP Secure Headers Project", "RFC 6797", "CSP Level 3", "RFC 6265bis"], vectors: 15, note: "M-merge done: csp-helper + cookie-flags + http-headers folded in. 25 headers, cookie hygiene, and CORS exposure; A-F grade." },
  { slug: "saml-decoder", name: "SAML decoder", family: "Security & WAF", status: "live", posture: "decode / explain", disposition: "built", specs: ["OASIS SAML 2.0", "W3C XML-DSig", "OWASP XXE"], vectors: 20, f5: true, note: "XXE-hardened (DOCTYPE rejected outright). Decodes Response/Assertion structure, signature posture, and weak-algo flags. Kept SEPARATE from xml-decoder (M6)." },
  { slug: "xml-decoder", name: "XML decoder", family: "Security & WAF", status: "live", posture: "decode / explain (XXE-hardened)", disposition: "built", vectors: 5, specs: ["W3C XML 1.0", "W3C Namespaces", "OWASP XXE"], note: "Kept SEPARATE from saml-decoder (M6). XXE-hardened mandatory golden vector." },

  // ---- QUEUED — Identity --------------------------------------------------
  { slug: "oidc", name: "OIDC decoder", family: "Identity & tokens", status: "live", posture: "decode / explain", disposition: "built", specs: ["OpenID Connect Core 1.0", "OpenID Connect Discovery 1.0", "RFC 6749", "RFC 7519"], vectors: 17, f5: true, note: "Decodes ID tokens (reuses the JWT engine) and .well-known/openid-configuration. Flags missing required claims, alg none, missing nonce, multi-aud without azp, and PKCE S256. Never calls the jwks_uri." },

  // ---- QUEUED — Protocol & packet decoders (decode-only) ------------------
  { slug: "layered-packet-decoder", name: "Layered packet decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only (spine)", disposition: "accept", rank: 42, note: "Family spine." },
  { slug: "layers-nest-explainer", name: "Layer-nesting explainer", family: "Protocol & packet decoders", status: "queued", posture: "explain (static)", disposition: "accept", rank: 119, note: "Kept SEPARATE (M3)." },
  { slug: "tcp-decoder", name: "TCP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 48, specs: ["RFC 9293"], note: "TCP-Options + TCP-RST folded in (decode only)." },
  { slug: "udp-decoder", name: "UDP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 50, specs: ["RFC 768"], isNew: true, note: "UDP family spine." },
  { slug: "dhcp-decoder", name: "DHCP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 53, specs: ["RFC 2131"], isNew: true, note: "UDP. No rogue-server / starvation tooling." },
  { slug: "dns-message-decoder", name: "DNS message decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only (local)", disposition: "accept", rank: 55, specs: ["RFC 1035"], isNew: true, note: "UDP. Local wire-format decode; DISTINCT from the deferred egress DNS lookup. No spoof / poisoning tooling." },
  { slug: "ntp-packet-decoder", name: "NTP packet decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 111, specs: ["RFC 5905"], isNew: true, note: "UDP. Complements epoch." },
  { slug: "vxlan-decoder", name: "VXLAN decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 112, specs: ["RFC 7348"], isNew: true, note: "UDP overlay encap (port 4789)." },
  { slug: "geneve-decoder", name: "GENEVE decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 113, specs: ["RFC 8926"], isNew: true, note: "UDP overlay encap; TLV options." },
  { slug: "quic-header-decoder", name: "QUIC header decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only (header-only)", disposition: "accept", rank: 114, specs: ["RFC 8999", "RFC 9000"], isNew: true, note: "UDP. Header / connection-ID decode only; payloads are encrypted — NO decryption." },
  { slug: "ospf-decoder", name: "OSPF decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 120, specs: ["RFC 2328"], note: "Decode only (no LSA craft / inject)." },
  { slug: "icmp-decoder", name: "ICMP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 57, specs: ["RFC 792", "RFC 4443"], note: "Decode only (no redirect craft)." },
  { slug: "bpdu-stp-decoder", name: "BPDU / STP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 121, specs: ["IEEE 802.1D", "IEEE 802.1Q"], note: "STP-translator folded in. Decode only (no superior-root craft)." },
  { slug: "pppoe-decoder", name: "PPPoE decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 122, specs: ["RFC 2516"], note: "Decode only (no CHAP crack)." },
  { slug: "lldp-cdp-decoder", name: "LLDP / CDP decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 123, specs: ["IEEE 802.1AB"], note: "Decode only (no spoof craft)." },
  { slug: "ipsec-decoder", name: "IPsec decoder", family: "Protocol & packet decoders", status: "queued", posture: "decode-only", disposition: "accept", rank: 124, specs: ["RFC 4301", "RFC 4303"], note: "Decode only (no ESP decrypt / key recover)." },

  // ---- QUEUED — Networking & addressing -----------------------------------
  { slug: "mac-oui", name: "MAC / OUI lookup", family: "Networking & addressing", status: "queued", posture: "lookup (vendored)", disposition: "accept", rank: 36, specs: ["IEEE OUI registry"], note: "Vendored IEEE OUI." },
  { slug: "network-number-registries", name: "Protocol-number registries", family: "Networking & addressing", status: "queued", posture: "lookup (vendored)", disposition: "accept", rank: 59, specs: ["IANA Protocol Numbers"], note: "'IP Protocols' folded in. Vendored IANA." },
  { slug: "ip-multicast-group", name: "IP multicast groups", family: "Networking & addressing", status: "queued", posture: "lookup / explain", disposition: "accept", rank: 64, specs: ["RFC 5771"] },
  { slug: "asn-prefix", name: "ASN / prefix origin", family: "Networking & addressing", status: "queued", posture: "analyze / explain", disposition: "accept", rank: 67, specs: ["RFC 4271", "RFC 6480"], note: "Merged: asn (local base) + route-object-prefix (M4). IRR / RPKI / bogon / aggregation." },
  // --- DNS-diagnostics cluster (PRIME 2026-06-30): interpret the TEXT OUTPUT of
  //     the two universal DNS CLIs, plus a command builder. Distinct from
  //     dns-message-decoder (binary wire-format hex) and dns-lookup (deferred,
  //     egress). All three are local/text-only with ZERO egress (D-49). Clustered
  //     at the top value tier (rank 4) so the two output explainers build together
  //     as one DNS unit; the builder rides slightly behind. ---
  { slug: "dig-output-explainer", name: "dig output explainer", family: "Networking & addressing", status: "live", posture: "decode / explain (local)", disposition: "built", vectors: 3, specs: ["RFC 1035", "RFC 6891", "RFC 2308", "RFC 8499"], isNew: true, note: "Paste real `dig` output; explains the header (opcode, status/RCODE, id, and the qr/aa/tc/rd/ra/ad/cd flags), the QUESTION/ANSWER/AUTHORITY/ADDITIONAL sections, every resource record (name, TTL, class, type, rdata) including SOA timers and DNSSEC RRs (RRSIG/DNSKEY/DS/NSEC), and the EDNS OPT pseudo-section + footer (query time, server, msg size). Parses the text only — never resolves anything (zero egress, D-49). DISTINCT from dns-message-decoder (wire-format hex)." },
  { slug: "nslookup-output-explainer", name: "nslookup output explainer", family: "Networking & addressing", status: "live", posture: "decode / explain (local)", disposition: "built", vectors: 4, specs: ["RFC 1035", "RFC 2308", "RFC 2782", "RFC 8499"], isNew: true, note: "Paste `nslookup` output; explains the Server/Address (which resolver answered), the meaning of 'Non-authoritative answer', the Name/Addresses and canonical-name (CNAME) chains, and the ways nslookup differs from dig (its own resolver, terser output, and the common 'authoritative vs cached' confusion). Text-only, zero egress (D-49). Pairs with dig-output-explainer." },
  { slug: "dig-command-builder", name: "dig command builder", family: "Networking & addressing", status: "queued", posture: "build / generate (local)", disposition: "accept", rank: 5, specs: ["RFC 1035", "RFC 6891", "RFC 4034"], isNew: true, note: "Inverse of the output explainer: assembles a correct `dig` command from intent — record type, @resolver, reverse (-x), +short / +trace / +dnssec / +tcp / +norecurse / +nssearch, EDNS bufsize, and port. Emits a copy-pasteable string only; runs nothing (zero egress, D-49). Fits the tcpdump / sniffer builder cluster." },

  // ---- QUEUED — HTTP & web ------------------------------------------------
  { slug: "http-methods-comparison", name: "HTTP methods comparison", family: "HTTP & web", status: "queued", posture: "explain", disposition: "accept", rank: 69, specs: ["RFC 9110"] },
  { slug: "http-request-translator", name: "HTTP request translator", family: "HTTP & web", status: "live", posture: "explain / translate", disposition: "built", vectors: 10, specs: ["curl manual", "RFC 9110"], isNew: true, note: "Paste a curl command and get it (a) explained (method, URL, every header, the body with its real Content-Type, auth, cookies, and each flag spelled out) and (b) translated to fetch (JS), a raw HTTP/1.1 request, HTTPie, and Python requests. One local parse (shell tokenizer + curl getopt) drives both views. Absorbs the former curl command explainer (merge M8). Gets curl's -d Content-Type default right (application/x-www-form-urlencoded, not JSON) and warns on the common mislabel, on -k / --insecure, on plaintext http, and on credentials in the URL. Distinct from http-message-decoder, which decodes raw HTTP messages rather than curl. Local, offline; the command, which may carry tokens or credentials, never leaves the browser." },
  { slug: "http-message-decoder", name: "HTTP message decoder", family: "HTTP & web", status: "queued", posture: "decode / explain", disposition: "accept", rank: 8, specs: ["RFC 9110", "RFC 9112"], isNew: true, note: "Paste a raw HTTP/1.1 request or response and get it decoded: the request line (method, target, version) or the status line, every header parsed and grouped, chunked transfer-encoding decoded and content-length checked, and any Authorization / Cookie / Set-Cookie surfaced with a hand-off to the JWT and cookie tools. The decode-only 'Inspector' half of Burp Suite and Fiddler, offline. No request is ever sent; the tool never replays, tampers, or fuzzes (D-53)." },
  { slug: "har-analyzer", name: "HAR analyzer", family: "HTTP & web", status: "queued", posture: "decode / explain / flag", disposition: "accept", rank: 21, specs: ["HAR 1.2"], isNew: true, note: "Paste or load a .har capture (the HTTP Archive JSON exported by Chrome DevTools, Firefox, Fiddler, or Charles) and get each entry decoded: method, URL, status, MIME and size, plus the waterfall timing breakdown (blocked / dns / connect / ssl / send / wait / receive). Flags redirect chains, missing compression, slow time-to-first-byte, and mixed content, and calls out the real hazard: HARs routinely leak Authorization headers, cookies, and tokens, so every credential-bearing entry is marked. Decode-only, offline; nothing in the capture leaves the browser." },
  { slug: "set-cookie-explainer", name: "Set-Cookie explainer", family: "HTTP & web", status: "queued", posture: "decode / explain / flag", disposition: "accept", rank: 16, specs: ["RFC 6265bis"], isNew: true, note: "Paste a Set-Cookie header and get every attribute explained (Domain, Path, Expires / Max-Age, Secure, HttpOnly, SameSite, Partitioned, and the __Host- / __Secure- prefixes) and flagged: missing Secure / HttpOnly / SameSite, over-broad Domain, and prefix violations. Decode and explain only, offline. Distinct from the F5 persistence-cookie decoder." },
  { slug: "f5-adv-waf-scanner-xml-explainer", name: "F5 Advanced WAF scanner-XML explainer", family: "Security & WAF", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 31, specs: ["F5 Advanced WAF generic scanner XML (schema pending)"], f5: true, isNew: true, note: "SCHEMA-PENDING (build gated on the F5 generic-format XSD/DTD from PRIME). Decodes the generic XML that F5 Advanced WAF / ASM uses to integrate external DAST scanners (Qualys, Rapid7, WhiteHat, IBM AppScan, Tenable): reads each vulnerability entry (attack type, URL, parameter, severity) and explains the mitigation ASM would derive from it. Sits on the XXE-safe xml-decoder substrate; decode-only, offline. To be grounded in the official F5 generic-scanner schema once supplied (compute, never guess)." },
  { slug: "cvss-vector-decoder", name: "CVSS vector decoder", family: "Security & WAF", status: "live", posture: "decode / score", disposition: "built", vectors: 10, specs: ["CVSS v3.1 (FIRST.org)"], isNew: true, note: "Paste a CVSS v3.1 vector (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) and get the Base score computed and mapped to None / Low / Medium / High / Critical, with Temporal and Environmental scores when those metrics are present, and every metric spelled out (attack vector, complexity, privileges required, user interaction, scope, and CIA impact). Pure scoring math, grounded in the open FIRST.org CVSS v3.1 specification (compute, never guess); the computational core that vulnerability findings, CVEs, and scanner reports all carry. Local, offline, no network." },
  { slug: "f5xc-was-report-explainer", name: "F5XC Web App Scanning report explainer", family: "Security & WAF", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 34, specs: ["F5XC Web App Scanning public API"], f5: true, isNew: true, note: "Paste an F5 Distributed Cloud Web App Scanning findings or report export (JSON) and get it rendered: each issue decoded with its CVSS vector (via the CVSS decoder), severity, vulnerable parameter / URL / method, CWE ID, and the OWASP Top Ten 2021 / API 2023 / LLM v1.1 categories the report carries, grouped by severity with remediation advice and resolved / accepted / rejected status. Bridges to the BIG-IP ASM export format (f5-adv-waf-scanner-xml-explainer). Decode-only; the report never leaves the browser. Grounded in the F5XC WAS public API schema." },
  { slug: "http-method-override", name: "HTTP method override", family: "HTTP & web", status: "queued", posture: "explain", disposition: "accept", rank: 70, specs: ["RFC 9110"] },
  { slug: "url-inspector", name: "URL inspector", family: "HTTP & web", status: "live", posture: "decode / explain", disposition: "built", specs: ["RFC 3986", "WHATWG URL", "RFC 3492"], vectors: 12, note: "Dissects a URL into named RFC 3986 components, splits the query into per-parameter percent-decoded pairs, decodes an internationalized host from punycode (RFC 3492), and flags embedded credentials, plaintext schemes, redundant ports, and the plus-as-space ambiguity. Faithful (no normalization); client-side only. Distinct from the Base64/percent codec." },

  // ---- QUEUED — TLS & transport -------------------------------------------
  { slug: "mtu-mss", name: "MTU / MSS reference", family: "TLS & transport", status: "queued", posture: "calculate / reference", disposition: "accept", rank: 73, specs: ["RFC 1191", "RFC 879"], note: "tunneling-overhead-reference folded in (M2)." },
  { slug: "bits-bytes", name: "Bits / bytes / throughput", family: "TLS & transport", status: "queued", posture: "convert", disposition: "accept", rank: 74, note: "Kept SEPARATE (M7): throughput-over-time vs bytes-on-wire." },

  // ---- QUEUED — Encoding --------------------------------------------------
  { slug: "charset-equivalency", name: "Charset equivalency", family: "Encoding", status: "queued", posture: "convert / explain", disposition: "accept", rank: 76, specs: ["Unicode", "RFC 3629"] },

  // ---- QUEUED — Identifiers & time ----------------------------------------
  { slug: "epoch", name: "Unix time converter", family: "Identifiers & time", status: "live", posture: "convert", disposition: "built", vectors: 7 },

  // ---- QUEUED — Text & utilities ------------------------------------------
  { slug: "regex", name: "Regex toolkit", family: "Text & utilities", status: "live", posture: "test / explain", disposition: "built", specs: ["ECMAScript regular expressions"], vectors: 24 },
  { slug: "log-parser", name: "Log parser", family: "Text & utilities", status: "queued", posture: "parse / explain", disposition: "accept", rank: 125 },
  { slug: "diff", name: "Text Diff", family: "Text & utilities", status: "live", posture: "compare / highlight", disposition: "built", vectors: true },
  { slug: "sed", name: "sed", family: "Text & utilities", status: "queued", posture: "transform", disposition: "accept", rank: 155, note: "Stream-editor evaluator: run a sed script against sample input. Heavier build (sed is a small language); deterministic + local once built. Evaluator class admitted per D-50 (PKG-001)." },
  { slug: "awk", name: "awk", family: "Text & utilities", status: "queued", posture: "transform", disposition: "accept", rank: 156, note: "Field/record evaluator: run an awk program against sample input. Heavier build (awk is a small language); deterministic + local once built. Evaluator class admitted per D-50 (PKG-001)." },
  { slug: "jq", name: "jq", family: "Text & utilities", status: "queued", posture: "transform", disposition: "accept", rank: 17, note: "JSON evaluator: run a jq filter against JSON input, entirely client-side (a WASM build of jq), deterministic and local. Evaluator class admitted per D-50 (PKG-001). Fills the gap of a privacy-first jq playground and complements the JSON tool cluster (json-formatter, json-yaml-convert, json-diff)." },
  { slug: "password-entropy", name: "Password entropy", family: "Text & utilities", status: "queued", posture: "analyze (analyzer-only)", disposition: "accept", rank: 15, note: "Analyzer only; never a cracker or wordlist." },
  { slug: "json-formatter", name: "JSON formatter & validator", family: "Text & utilities", status: "live", posture: "format / validate", disposition: "built", specs: ["RFC 8259", "ECMA-404", "RFC 6901"], vectors: 19, note: "Hand-written position-tracking parser: pretty-print, minify, sort keys, with precise errors (line, column, and RFC 6901 pointer), duplicate-key detection, and big-number fidelity that a JSON.parse round-trip would destroy. Client-side only. Foundation for the JSON/YAML family." },
  { slug: "json-yaml-convert", name: "JSON ↔ YAML converter", family: "Text & utilities", status: "live", posture: "convert", disposition: "built", specs: ["YAML 1.2.2", "RFC 8259"], vectors: 16, f5: true, note: "Bidirectional JSON/YAML. JSON validated by the json-formatter parser (precise errors); YAML handled by js-yaml under its JSON-compatible schema (anchors expanded, Norway-problem strings auto-quoted). Conversion notes flag dropped comments, expanded anchors, literal merge keys, and number-precision limits. Client-side. F5 AS3/DO (JSON) ↔ k8s/Ansible/CI (YAML)." },
  { slug: "json-diff", name: "Semantic JSON diff", family: "Text & utilities", status: "queued", posture: "compare", disposition: "accept", rank: 32, note: "Key-aware structural diff (order-insensitive for objects), distinct from the generic text Diff (rank 7). Ideal for comparing two config/declaration snapshots." },
  { slug: "yaml-validator", name: "YAML validator & footgun check", family: "Text & utilities", status: "queued", posture: "validate / explain", disposition: "accept", rank: 43, note: "Validates YAML and flags the classic footguns: tabs, the Norway problem (no -> false), duplicate keys, ambiguous unquoted scalars, anchor/alias misuse. Strong Learn-article material." },
  { slug: "json-schema-validator", name: "JSON Schema validator", family: "Text & utilities", status: "queued", posture: "validate", disposition: "accept", rank: 68, f5: true, note: "Validate a JSON document against a JSON Schema, with readable error paths. F5 publishes AS3/DO schemas, so this doubles as a declaration linter. Heavier build (draft 2020-12); lower priority than the formatter/converter." },

  // ============================================================================
  // QUEUED — RATIFIED F5 EXPANSION (ratified 27/06/2026)
  // All decode / explain / convert / validate; deterministic; local; public-docs
  // anchored. No forge / crack / evade / defeat. Version-dependent data (cipher
  // DB, signature metadata, model specs) ships as vendored, dated snapshots.
  // ============================================================================

  // ---- F5 LTM, iRules & platform -----------------------------------------
  { slug: "f5-irules-event-order", name: "iRule event order", family: "F5 LTM, iRules & platform", status: "live", posture: "explain (deterministic)", disposition: "built", vectors: 5, specs: ["F5 iRules / TMOS event model"], f5: true },
  { slug: "virtual-server-match-order", name: "Virtual-server match-order explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 78, specs: ["F5 LTM virtual server selection"], f5: true, note: "Most-specific destination/port/source precedence: which VIP wins for a given packet." },
  { slug: "health-monitor-timing", name: "Health-monitor timing calculator", family: "F5 LTM, iRules & platform", status: "queued", posture: "calculate / explain", disposition: "accept", rank: 79, specs: ["F5 LTM monitors"], f5: true, note: "interval / timeout / up-interval math (timeout >= 3*interval + 1); send/recv explainer." },
  { slug: "f5-tcpdump-syntax", name: "F5 tcpdump syntax builder", family: "F5 LTM, iRules & platform", status: "dropped", posture: "build / explain (never captures)", disposition: "drop", specs: ["F5 tcpdump -i 0.0:nnn syntax"], f5: true, note: "Superseded: built as the live f5-bigip-tcpdump-builder, which is exactly this tool (the F5 interface syntax, the :p / :n flow flags, and command assembly) under its final name. Consolidated in the PKG-002 pass, 2026-06-30." },
  { slug: "snat-port-exhaustion", name: "SNAT port-exhaustion calculator", family: "F5 LTM, iRules & platform", status: "queued", posture: "calculate / explain", disposition: "accept", rank: 82, specs: ["F5 SNAT automap / SNAT pool"], f5: true, note: "N SNAT addresses -> max concurrent flows to one destination; automap vs SNAT pool." },
  { slug: "f5-persistence-method-explainer", name: "Persistence-method explainer", family: "F5 LTM, iRules & platform", status: "live", posture: "explain", disposition: "built", specs: ["F5 LTM persistence"], vectors: 14, f5: true, note: "Paste persistence profiles and virtual servers for the method behind each (cookie, source-address, dest-address, SSL, universal, hash, MSRDP, SIP), its failure modes, and each virtual's resolved primary-to-fallback chain. Reuses the tmsh parser and complements f5-bigip-persistence-cookie." },
  { slug: "f5-service-check-date", name: "F5 service check date", family: "F5 LTM, iRules & platform", status: "live", posture: "compute / explain", disposition: "built", specs: ["F5 K7727", "F5 K8986", "F5 K3782", "F5 K000160443"], vectors: 20, f5: true, note: "Two-way lookup over F5's published License Check Date table (K7727): enter a BIG-IP version for the minimum service check date its license must carry, or enter a service check date for the newest version you can upgrade to and the newer branches you cannot reach yet. Also reads a pasted /config/bigip.license or tmsh show sys license output. Runs entirely in the browser." },
  { slug: "f5-bigip-license-explainer", name: "F5 BIG-IP license explainer", family: "F5 LTM, iRules & platform", status: "live", posture: "decode / explain", disposition: "built", specs: ["F5 K000160443", "F5 K7727", "F5 K3782", "F5 K7752", "F5 K42091606", "F5 K02011230", "F5 K15643"], vectors: 6, f5: true, note: "Paste /config/bigip.license, full file or fragment, and read it in plain language: BIG-IQ vs direct management, licensing dates with the K7727 upgrade verdict, Registration Key and platform, active and optional modules with per-module keys, Exclusive and Deny constraints, and every feature token. Key and signature values are never displayed. Runs entirely in the browser." },
  { slug: "f5-tmsh-config-explainer", name: "tmsh config explainer", family: "F5 LTM, iRules & platform", status: "live", posture: "decode / explain", disposition: "built", specs: ["F5 tmsh reference"], vectors: 14, f5: true, note: "Paste a bigip.conf snippet for a plain-English breakdown of every object plus the structure. Recognizes virtual servers, pools, nodes, monitors, profiles, persistence, SNAT, self IPs, VLANs, and routes; captures iRule Tcl verbatim; flags operational risks such as an HTTPS virtual with no client-SSL profile or a pool with no monitor." },
  { slug: "f5-irules-command-context", name: "iRules command / context explainer", family: "F5 LTM, iRules & platform", status: "live", posture: "reference / validate", disposition: "built", specs: ["F5 iRules API reference"], vectors: 18, f5: true, note: "Paste an iRule for per-block event identities (the Master List's own one-liners, curated LTM-scope table), command inventories with direct reference-page links, the documented priority evaluation order (0-1000, default 500, lower first, insertion order breaks ties), and the CMP audit sourced to the CMP Compatibility page: global-variable demotion (validator catches as of v10), the static:: alternative, RULE_INIT per-TMM keys, per-TMM statistics. Deliberately links per-command validity pages rather than reproducing tables it has not verified; the per-command validity matrix is the documented roadmap enhancement. Built as f5-irules-command-context per D-78." },
  { slug: "route-domain-partition-decoder", name: "Route-domain / partition decoder", family: "F5 LTM, iRules & platform", status: "queued", posture: "decode / explain", disposition: "accept", rank: 83, specs: ["F5 route domains / partitions"], f5: true, note: "Unpacks the %RD suffix and /partition/ folder notation in object names and IPs." },
  { slug: "f5-lb-method-chooser", name: "LB-method chooser", family: "F5 LTM, iRules & platform", status: "live", posture: "explain", disposition: "built", specs: ["F5 LTM load-balancing methods"], vectors: 20, f5: true, note: "Paste an ltm pool for its load-balancing method explained in F5's own terms with cross-checks against the rest of the pool (ratios the mode ignores, missing connection limits, slow-ramp pairing, priority-group activation), or answer two questions for a sourced recommendation. All 19 documented modes. Reuses the tmsh parser. Built as f5-lb-method-chooser per the D-78 prefix discipline." },
  { slug: "tmsh-irest-mapper", name: "tmsh <-> iControl REST path mapper", family: "F5 LTM, iRules & platform", status: "queued", posture: "convert / explain", disposition: "accept", rank: 85, specs: ["F5 iControl REST"], f5: true, note: "ltm pool /Common/web <-> /mgmt/tm/ltm/pool/~Common~web. Automation aid." },
  { slug: "ha-model-explainer", name: "HA / failover explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain", disposition: "accept", rank: 86, specs: ["F5 device service clustering"], f5: true, note: "Device groups, traffic groups, MAC masquerade, connection mirroring, failover triggers." },
  { slug: "f5-irules-vs-ltm-policy", name: "iRules vs LTM Policy classifier", family: "F5 LTM, iRules & platform", status: "live", posture: "explain", disposition: "built", specs: ["F5 LTM policies", "F5 iRules"], vectors: 16, f5: true, note: "Paste an iRule and every when block gets one of three honest verdicts: policy-expressible (mapped onto the grammar the vendor's own 12.1 examples demonstrate, with a migration sketch), verify-on-version (plausible constructs the verified sources did not demonstrate, HTTP::redirect the canonical resident), or iRule-required with the blockers named (events beyond HTTP request/response, loops, regex logic, variables, table/session state, sideband, payload collection). The strategies keyword renders first-match / all-match / best-match verbatim plus the precedence-table head. Built as f5-irules-vs-ltm-policy per D-78." },
  { slug: "oneconnect-source-mask", name: "OneConnect source-mask explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain", disposition: "accept", rank: 89, specs: ["F5 OneConnect profile"], f5: true, note: "What 255.255.255.255 vs 0.0.0.0 masks do to connection reuse." },

  // ---- F5 GTM, AFM & APM --------------------------------------------------
  { slug: "afm-rule-context", name: "AFM rule-context & match explainer", family: "F5 GTM, AFM & APM", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 90, specs: ["F5 BIG-IP AFM"], f5: true, note: "Enforcement hierarchy (global -> route-domain -> virtual -> self-IP) + which rule matches a packet." },
  { slug: "packet-filter-explainer", name: "BIG-IP packet-filter explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 91, specs: ["F5 net packet-filter (tmsh)", "pcap-filter(7)"], f5: true, note: "Paste net packet-filter rules for the ordered first-match walk (action/order/vlan), the tcpdump-syntax rule expressions decoded (engine synergy with the tcpdump builder), and a which-rule-matches-this-packet simulator. Marquee semantics pinned at source: trusted exemptions (net packet-filter-trusted) process BEFORE rule evaluation and cannot be overridden by any rule; the global Packet Filtering master switch off means all rules inert and all traffic allowed; an empty rule expression matches ALL packets. Distinguishes the classic net packet-filter from the v16+ security packet-filter policy namesake (IPv6 extension-header filtering). Ships with a rich paired Learn article (bigip-packet-filters): mechanism, evaluation order, global Properties/Exemptions, the tcpdump grammar, and the two-objects-one-name trap. Born as f5-packet-filter-explainer per D-78." },
  { slug: "f5-gslb-decision-flow", name: "GSLB decision-flow explainer", family: "F5 GTM, AFM & APM", status: "live", posture: "explain", disposition: "built", specs: ["F5 BIG-IP DNS / GTM"], vectors: 21, f5: true, note: "Paste gtm wideip and gtm pool stanzas for the two-tier decision flow explained in F5's own terms: pool selection at the wide IP, then the preferred/alternate/fallback chain with the grammar validated per tier, the fallback-ignores-availability rule stated, and the Load Balancing manual's cross-checks (Fallback IP wiring, dynamic-ratio scope, the topology-at-both-tiers warning). Reuses the tmsh parser. Built as f5-gslb-decision-flow per the D-78 prefix discipline." },
  { slug: "iquery-protocol-explainer", name: "iQuery protocol explainer", family: "F5 GTM, AFM & APM", status: "queued", posture: "explain / troubleshoot (static)", disposition: "accept", rank: 2, specs: ["F5 BIG-IP DNS (GTM) iQuery", "F5 Device Trust / CMI"], f5: true, isNew: true, note: "Explains F5's proprietary iQuery protocol (TCP 4353, XML over mutually-authenticated SSL): the big3d agent and gtmd roles, the metrics/health it carries, and its three jobs - GTM server/datacenter probing, Device Trust (CMI) establishment, and ConfigSync transport. Centers on the #1 GTM/DNS pain point: reading green / blue / red server status and diagnosing 'iQuery not connected' (cert/trust, port 4353 reachability, time skew, big3d version). Static explainer - runs nothing, zero egress (D-49). DISTINCT from ha-model-explainer (HA concepts) and gslb-decision-flow (LB logic)." },
  { slug: "f5-topology-longest-match", name: "GTM topology longest-match scorer", family: "F5 GTM, AFM & APM", status: "live", posture: "explain (deterministic)", disposition: "built", specs: ["F5 GTM topology records"], vectors: 18, f5: true, note: "Paste gtm topology records (and gtm region stanzas), declare a source, and the decision computes the way BIG-IP DNS computes it: the Longest Match sort with per-record rank rationale (K10721's ladder and negation buckets), the first-match-per-candidate scoring walk with shadowing shown, highest score wins and ties round-robin. Built as f5-topology-longest-match per the D-78 prefix discipline." },
  { slug: "apm-sso-explainer", name: "APM SSO method explainer", family: "F5 GTM, AFM & APM", status: "queued", posture: "explain", disposition: "accept", rank: 93, specs: ["F5 BIG-IP APM"], f5: true, note: "forms / Kerberos-constrained-delegation / NTLM / header / SAML / OAuth-bearer; per-request vs per-session." },
  { slug: "apm-session-variable-reference", name: "APM session-variable reference", family: "F5 GTM, AFM & APM", status: "queued", posture: "reference (vendored)", disposition: "accept", rank: 94, specs: ["F5 APM session variables"], f5: true, note: "Searchable map of session.* variables. Vendored snapshot." },
  { slug: "f5-dos-vector-explainer", name: "AFM DoS-vector / profile explainer", family: "F5 GTM, AFM & APM", status: "live", posture: "explain (defensive config only)", disposition: "built", specs: ["F5 AFM DoS profiles"], vectors: 21, f5: true, note: "Paste security dos device-config or profile stanzas for every vector explained in F5's own one-liners (the full 105-entry reference table, sys-db tunables included) with the threshold mechanics spelled out (1-minute averages vs learned 1-hour baselines) and deterministic cross-checks: the mitigation-below-detection inversion that drops silently, automatic-mode semantics, detection-disabled policing, bad-actor wiring, and the tcp-half-open SYN-cookie interplay. Defensive config only; never generates traffic. Built as f5-dos-vector-explainer per the D-78 prefix discipline." },

  // ---- F5 automation (AS3 / DO) ------------------------------------------
  { slug: "as3-explainer-validator", name: "AS3 declaration explainer + validator", family: "F5 automation (AS3 / DO)", status: "queued", posture: "validate / explain (schema)", disposition: "accept", rank: 96, specs: ["F5 AS3 schema"], f5: true, note: "Schema-validate + tenant->app->service tree + objects created. Flags TLS_Server=client-ssl gotcha. AS3 covers LTM/GTM/APM/AFM/ASM/PEM." },
  { slug: "do-explainer-validator", name: "DO declaration explainer + validator", family: "F5 automation (AS3 / DO)", status: "queued", posture: "validate / explain (schema)", disposition: "accept", rank: 97, specs: ["F5 Declarative Onboarding schema"], f5: true, note: "System/VLAN/self-IP/route/license/provision; makes the DO-vs-AS3 boundary explicit." },
  { slug: "ts-explainer", name: "Telemetry Streaming explainer", family: "F5 automation (AS3 / DO)", status: "queued", posture: "explain (schema)", disposition: "accept", rank: 126, specs: ["F5 Telemetry Streaming schema"], f5: true, note: "What stats/events a TS declaration forwards and where (Splunk/ELK/Grafana)." },
  { slug: "iapps-legacy-pointer", name: "iApps legacy pointer", family: "F5 automation (AS3 / DO)", status: "queued", posture: "explain", disposition: "accept", rank: 127, specs: ["F5 iApps / AS3 / FAST"], f5: true, note: "iApps are legacy; AS3 + FAST is the modern declarative path." },

  // ---- F5 ASM additions (Security & WAF family) --------------------------
  { slug: "asm-violation-signature-explainer", name: "ASM violation / signature explainer", family: "Security & WAF", status: "queued", posture: "decode / explain", disposition: "accept", rank: 117, specs: ["F5 BIG-IP ASM / Advanced WAF"], f5: true, note: "Explains violations / signature categories (not evasion). Vendored signature-metadata snapshot." },
  { slug: "asm-support-id-decoder", name: "ASM support-ID decoder", family: "Security & WAF", status: "queued", posture: "decode", disposition: "accept", rank: 118, specs: ["F5 BIG-IP ASM"], f5: true, note: "Unpacks an ASM support ID for log correlation." },

  // ---- F5 TLS additions (TLS & transport family) ------------------------
  { slug: "f5-cipher-string-expander", name: "F5 cipher-string explainer", family: "TLS & transport", status: "live", posture: "explain / analyze", disposition: "built", specs: ["F5 cipher rules/groups", "tmm --clientciphers"], vectors: 14, f5: true, note: "Parses an F5 cipher string (or a pre-built rule name), explains every keyword and operator, and flags weak choices and forward secrecy. Honest scope: does not reproduce the exact per-TMOS ordered suite list, which needs tmm --clientciphers on the target version. Pairs with the live cipher-suite decoder." },
  { slug: "f5-ssl-profile-explainer", name: "F5 SSL profile explainer", family: "TLS & transport", status: "live", posture: "explain", disposition: "built", vectors: 4, specs: ["F5 client-ssl / server-ssl profiles"], f5: true, note: "Chain building, SNI, renegotiation, OCSP stapling; client-ssl vs server-ssl." },

  // ============================================================================
  // QUEUED — RATIFIED FORTINET EXPANSION (ratified 27/06/2026)
  // Same posture: decode / explain / convert / validate; deterministic; local;
  // public-docs anchored (FortiOS 7.6.x). No forge / crack / evade / decrypt.
  // Version-dependent data (FortiGuard categories, signature metadata, model
  // specs, log-IDs) ships as vendored, dated snapshots pinned to a FortiOS line.
  // ============================================================================

  // ---- Fortinet FortiGate & FortiOS --------------------------------------
  { slug: "fortigate-policy-match-order", name: "FortiGate policy match-order explainer", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 98, specs: ["FortiOS firewall policy"], fortinet: true, note: "Top-down first-match + implicit deny; sequence-based vs NGFW/policy-based modes. Which rule wins for a packet." },
  { slug: "fortios-cli-config-explainer", name: "FortiOS CLI config explainer", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "decode / explain", disposition: "accept", rank: 99, specs: ["FortiOS CLI reference"], fortinet: true, note: "Paste a config/edit/set/next/end block -> plain-English breakdown + structure tree." },
  { slug: "fortigate-session-decoder", name: "FortiGate session-table decoder", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "decode / explain", disposition: "accept", rank: 100, specs: ["FortiOS diag sys session list"], fortinet: true, note: "Decode a session entry: proto, src->dst, state, NAT, expiry, helper, hook, gateway, policy id, NPU flags." },
  { slug: "fortigate-vdom-explainer", name: "FortiGate VDOM explainer", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "explain", disposition: "accept", rank: 101, specs: ["FortiOS VDOMs"], fortinet: true, note: "VDOMs, inter-VDOM links, management VDOM, split-task vs multi-VDOM." },
  { slug: "fortios-routing-explainer", name: "FortiOS route-lookup explainer", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "explain", disposition: "accept", rank: 102, specs: ["FortiOS routing"], fortinet: true, note: "Routing table + policy routes (PBR) precedence, ECMP, distance/priority, SD-WAN interaction." },
  { slug: "fortios-npu-offload", name: "FortiGate NP7 / SOC offload explainer", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "explain", disposition: "accept", rank: 103, specs: ["FortiGate NP7 / SOC4"], fortinet: true, note: "What gets hardware-offloaded vs punted to CPU, and what breaks offload (IPS, proxy mode)." },

  // ---- Fortinet security, VPN & SD-WAN -----------------------------------
  { slug: "fortios-security-profiles", name: "Security-profile / inspection-mode explainer", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain", disposition: "accept", rank: 104, specs: ["FortiOS security profiles"], fortinet: true, note: "flow vs proxy inspection + the profile stack (AV / IPS / web filter / app control / DNS filter / file filter / DLP)." },
  { slug: "fortigate-ssl-inspection", name: "SSL/SSH inspection explainer", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain (config only)", disposition: "accept", rank: 105, specs: ["FortiOS SSL/SSH inspection"], fortinet: true, note: "certificate-inspection vs deep-inspection, CA cert, exemptions. Explains config; never decrypts." },
  { slug: "fortigate-ipsec-p1p2", name: "IPsec phase1/phase2 mismatch checker", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain / compare (deterministic)", disposition: "accept", rank: 106, specs: ["RFC 7296", "FortiOS IPsec VPN"], fortinet: true, note: "Paste both peers' proposals -> find the mismatch. Note 7.6.1 IKE-over-TCP default port 443." },
  { slug: "fortigate-sdwan-rule", name: "SD-WAN rule / SLA explainer", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 107, specs: ["FortiOS SD-WAN"], fortinet: true, note: "Rule strategy (manual / best-quality / lowest-cost / maximize-bandwidth), Performance SLA thresholds, rule match order." },
  { slug: "fortigate-sdwan-sla-calc", name: "SD-WAN SLA target calculator", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "calculate", disposition: "accept", rank: 108, specs: ["FortiOS SD-WAN health-check"], fortinet: true, note: "Given thresholds + measured latency/jitter/loss, which members are in or out of SLA." },
  { slug: "fortigate-sslvpn-explainer", name: "SSL-VPN / Agentless-VPN explainer", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain", disposition: "accept", rank: 109, specs: ["FortiOS SSL-VPN"], fortinet: true, note: "Modes + the deprecation (SSL-VPN web mode is now 'Agentless VPN', unsupported on some models)." },
  { slug: "fortigate-ztna-explainer", name: "ZTNA tag / posture explainer", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "explain", disposition: "accept", rank: 128, specs: ["FortiOS ZTNA"], fortinet: true, note: "ZTNA tags, access proxy, posture checks." },
  { slug: "fortiguard-category-lookup", name: "FortiGuard category lookup", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "lookup (vendored)", disposition: "accept", rank: 129, specs: ["FortiGuard web-filter / app-control categories"], fortinet: true, note: "Category ID -> name/group. Vendored snapshot. (Live URL classification is egress -> deferred.)" },
  { slug: "fortigate-log-decoder", name: "FortiGate log decoder", family: "Fortinet security, VPN & SD-WAN", status: "queued", posture: "decode / explain", disposition: "accept", rank: 110, specs: ["FortiOS log reference"], fortinet: true, note: "Decode a log line by logid + field reference (type/subtype, srcip/dstip/policyid/action). Vendored log-ID snapshot." },

  // ---- Fortinet ecosystem & identity -------------------------------------
  { slug: "fortiweb-policy-explainer", name: "FortiWeb policy / signature explainer", family: "Fortinet ecosystem & identity", status: "queued", posture: "decode / explain", disposition: "accept", rank: 130, specs: ["FortiWeb"], fortinet: true, note: "Server policies, protection profiles, ML anomaly detection. The Fortinet parallel to asm-waf-inspector. Vendored signature metadata." },
  { slug: "fortiadc-lb-explainer", name: "FortiADC LB-method / health-check explainer", family: "Fortinet ecosystem & identity", status: "queued", posture: "explain", disposition: "accept", rank: 131, specs: ["FortiADC"], fortinet: true, note: "Virtual servers, real servers, methods, persistence, health checks. Parallel to the F5 LTM LB tools." },
  { slug: "fortimanager-adom-explainer", name: "FortiManager ADOM / policy-package explainer", family: "Fortinet ecosystem & identity", status: "queued", posture: "explain", disposition: "accept", rank: 132, specs: ["FortiManager"], fortinet: true, note: "ADOMs, policy packages, device DB vs ADOM DB, install targets." },
  { slug: "fortisase-explainer", name: "FortiSASE component explainer", family: "Fortinet ecosystem & identity", status: "queued", posture: "explain", disposition: "accept", rank: 133, specs: ["FortiSASE"], fortinet: true, note: "SWG / ZTNA / CASB / FWaaS architecture." },
  { slug: "fortigate-scim-explainer", name: "SCIM 2.0 provisioning explainer", family: "Fortinet ecosystem & identity", status: "queued", posture: "explain", disposition: "accept", rank: 134, specs: ["SCIM 2.0", "RFC 7644"], fortinet: true, note: "FortiOS 7.6 SCIM server: auto-provision users/groups from an IdP. Pairs with the queued SAML/OIDC decoders." },
  { slug: "totp-hotp", name: "TOTP / HOTP", family: "Identity & tokens", status: "live", posture: "generate / verify", disposition: "built", specs: ["RFC 6238", "RFC 4226"], vectors: true },

  // ============================================================================
  // QUEUED — RATIFIED NETSKOPE + EXTREME EXPANSION (ratified 27/06/2026)
  // Same posture: decode / explain / convert / validate; deterministic; local;
  // public-docs anchored. No forge / crack / evade / decrypt. Version-dependent
  // data (categories, CCI, classifier lists, log/event IDs, model/OS maps) ships
  // as vendored, dated snapshots.
  // ============================================================================

  // ---- Netskope SSE / SASE (build-first 4 + 4 others) --------------------
  { slug: "pac-file-explainer", name: "PAC file explainer + validator", family: "Netskope SSE / SASE", status: "queued", posture: "parse / validate", disposition: "accept", rank: 115, specs: ["PAC (FindProxyForURL)"], netskope: true, note: "Parses FindProxyForURL logic (isInNet, dnsDomainIs, ...). Netskope steering aid; cross-platform standard." },
  { slug: "sse-architecture-explainer", name: "SSE / SASE single-pass architecture explainer", family: "Netskope SSE / SASE", status: "queued", posture: "explain", disposition: "accept", rank: 135, specs: ["Netskope One platform"], netskope: true, note: "Zero Trust Engine, single-pass inspection order, the SSE stack (SWG/CASB/ZTNA/DLP/FWaaS/RBI/DNSaaS)." },
  { slug: "netskope-steering-explainer", name: "Netskope steering-method explainer", family: "Netskope SSE / SASE", status: "queued", posture: "explain", disposition: "accept", rank: 136, specs: ["Netskope One steering"], netskope: true, note: "Client vs IPsec vs GRE vs proxy-chaining vs DNS steering to NewEdge; when each applies." },
  { slug: "netskope-dlp-concepts", name: "Netskope DLP concept explainer", family: "Netskope SSE / SASE", status: "queued", posture: "explain", disposition: "accept", rank: 137, specs: ["Netskope One DLP"], netskope: true, note: "Rules, classifiers, file types, EDM, fingerprinting. Explains concepts; never builds classifiers or extracts data." },
  { slug: "netskope-cci-explainer", name: "Cloud Confidence Index (CCI) explainer", family: "Netskope SSE / SASE", status: "queued", posture: "reference (vendored)", disposition: "accept", rank: 148, specs: ["Netskope Cloud Confidence Index"], netskope: true, note: "App-risk scoring model + attributes. Vendored reference. (Non-build-first: queued at end.)" },
  { slug: "netskope-ztna-explainer", name: "Netskope Private Access (ZTNA) explainer", family: "Netskope SSE / SASE", status: "queued", posture: "explain", disposition: "accept", rank: 149, specs: ["Netskope One Private Access"], netskope: true, note: "Publishers, app definitions, ZTNA-vs-VPN, bi-directional ZTNA Next. (Non-build-first: queued at end.)" },
  { slug: "netskope-log-fields", name: "Netskope log / event field reference", family: "Netskope SSE / SASE", status: "queued", posture: "decode / reference (vendored)", disposition: "accept", rank: 150, specs: ["Netskope SkopeIT events"], netskope: true, note: "Decode a page/application event's fields. Vendored field map. (Non-build-first: queued at end.)" },
  { slug: "netskope-sso-explainer", name: "Netskope SSO / SCIM integration explainer", family: "Netskope SSE / SASE", status: "queued", posture: "explain", disposition: "accept", rank: 151, specs: ["SAML 2.0", "OIDC", "SCIM 2.0"], netskope: true, note: "Netskope as SP + SCIM provisioning. Shares standards with the queued saml/oidc decoders. (Non-build-first: queued at end.)" },

  // ---- Extreme switching & fabric (EXOS / VOSS / SPB) --------------------
  { slug: "exos-config-explainer", name: "EXOS (Switch Engine) config explainer", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "decode / explain", disposition: "accept", rank: 138, specs: ["ExtremeXOS / Switch Engine CLI"], extreme: true, note: "Paste an EXOS config -> plain-English + structure tree." },
  { slug: "fabric-connect-spb-explainer", name: "Fabric Connect / SPB explainer", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "decode / explain", disposition: "accept", rank: 139, specs: ["IEEE 802.1aq (SPB)", "IS-IS"], extreme: true, note: "Shortest Path Bridging + IS-IS: BVLANs, I-SIDs, B-MAC, L2VSN/L3VSN. Extreme's signature tech; rare tooling." },
  { slug: "voss-config-explainer", name: "VOSS (Fabric Engine) config explainer", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "decode / explain", disposition: "accept", rank: 141, specs: ["VOSS / Fabric Engine ACLI"], extreme: true, note: "Same for the Avaya-lineage ACLI syntax (distinct grammar from EXOS)." },
  { slug: "extremecontrol-nac-explainer", name: "ExtremeControl NAC policy explainer", family: "Extreme NAC & cloud (ExtremeControl / Platform ONE)", status: "queued", posture: "explain", disposition: "accept", rank: 145, specs: ["ExtremeControl"], extreme: true, note: "Roles, contextual identity; policies combine VLAN/L2VSN/L3VSN/L2-L7 ACL+QoS and convert to dACLs." },
  { slug: "isid-vsn-decoder", name: "I-SID / VSN decoder", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "decode", disposition: "accept", rank: 142, specs: ["SPBM I-SID / VSN"], extreme: true, note: "Decode a Service Instance Identifier + its L2VSN/L3VSN mapping." },
  { slug: "extreme-switch-os-mapper", name: "Universal switch OS-name mapper", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "reference (vendored)", disposition: "accept", rank: 140, specs: ["Extreme Universal switches"], extreme: true, note: "EXOS<->Switch Engine / VOSS<->Fabric Engine, by model. Timely (image file names are still EXOS/VOSS)." },
  { slug: "extreme-dacl-explainer", name: "Downloadable ACL (dACL) explainer", family: "Extreme NAC & cloud (ExtremeControl / Platform ONE)", status: "queued", posture: "decode / explain", disposition: "accept", rank: 146, specs: ["ExtremeControl dACL"], extreme: true, note: "The downloadable-ACL structure ExtremeControl pushes to switches and routers." },
  { slug: "exos-voss-mapper", name: "EXOS <-> VOSS concept mapper", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "convert / explain", disposition: "accept", rank: 143, specs: ["ExtremeXOS", "VOSS"], extreme: true, note: "VLAN vs VSN and friends, for shops running both OSes." },
  { slug: "fabric-attach-explainer", name: "Fabric Attach explainer", family: "Extreme switching & fabric (EXOS / VOSS / SPB)", status: "queued", posture: "explain", disposition: "accept", rank: 144, specs: ["Extreme Fabric Attach"], extreme: true, note: "Auto edge-to-fabric provisioning." },
  { slug: "extreme-platform-map", name: "Platform ONE / XIQ / Site Engine map", family: "Extreme NAC & cloud (ExtremeControl / Platform ONE)", status: "queued", posture: "explain", disposition: "accept", rank: 147, specs: ["Extreme Platform ONE", "ExtremeCloud IQ"], extreme: true, note: "The cloud-vs-on-prem management split and where ExtremeControl/Analytics live." },

  // ---- Python family (added 2026-06-30, D-51 admission; all local / zero-egress, D-49) --
  { slug: "python-playground", name: "Python playground", family: "Python", status: "queued", posture: "evaluate", disposition: "accept", rank: 6, specs: ["CPython (via Pyodide / WASM)"], isNew: true, note: "Run Python entirely in the browser via Pyodide (CPython compiled to WASM), so code and data never leave the device (zero egress, D-49). Evaluator class (D-50), alongside regex / jq / sed / awk. Heaviest build on the site: the WASM runtime is multi-megabyte and lazy-loads only when the tool opens. Deterministic modulo time / random (UI-layer inputs)." },
  { slug: "requirements-txt-explainer", name: "requirements.txt explainer", family: "Python", status: "queued", posture: "decode / explain (local)", disposition: "accept", rank: 20, specs: ["PEP 508", "PEP 440"], isNew: true, note: "Parses each requirement into name, extras, version specifier, and environment marker, and explains them - including the gotchas (== is not a literal pin, === for byte-exact, pre / dev-release ordering, ^ is not PEP 440). Handles pip lines (-r, -e, --hash, index URLs). Pure text parse, never resolves from PyPI (zero egress, D-49)." },
  { slug: "python-traceback-explainer", name: "Python traceback explainer", family: "Python", status: "queued", posture: "decode / explain (local)", disposition: "accept", rank: 25, specs: ["CPython traceback format"], isNew: true, note: "Paste a traceback; surfaces the exception type and message, the frame chain (file, line, function), the deepest (usually culprit) frame, chained-exception structure (During handling of the above exception...), and common-cause hints for frequent exceptions. Pure text parse (zero egress, D-49)." },
  { slug: "pyproject-toml-explainer", name: "pyproject.toml explainer", family: "Python", status: "queued", posture: "decode / explain / validate (local)", disposition: "accept", rank: 27, specs: ["PEP 621", "PEP 518", "PEP 508"], isNew: true, note: "Explains [build-system] (requires + build-backend, PEP 518), [project] metadata (name, version, dependencies, optional-dependencies, requires-python, entry points, PEP 621), and the [tool.*] tables, and validates the [project] table. Parses TOML locally (zero egress, D-49)." },
  { slug: "wheel-filename-explainer", name: "Wheel filename & PEP 440 version explainer", family: "Python", status: "queued", posture: "decode / explain (local)", disposition: "accept", rank: 49, specs: ["PEP 427", "PEP 440"], isNew: true, note: "Decodes a .whl filename into its distribution / version / build / python / abi / platform tags (cp311, abi3, manylinux / macosx / win, none-any), and separately parses, compares, and normalizes PEP 440 versions (epoch, release, pre / post / dev, local). Pure parse (zero egress, D-49)." },
  // ---- Formerly deferred: egress tools DROPPED (revisit if/when egress is built); extended tiers brought into the T0 queue (D-52) --
  { slug: "dns-lookup", name: "DNS lookup (egress)", family: "Networking & addressing", status: "dropped", posture: "egress (single / bulk / monitor)", disposition: "drop", note: "Dropped: requires network egress, so it cannot run in the T0 local surface. Revisit if/when egress (a hosted API / T2) is built." },
  { slug: "asn-live", name: "ASN live (egress)", family: "Networking & addressing", status: "dropped", posture: "egress (live BGP / IRR)", disposition: "drop", note: "Dropped: requires live network egress (BGP / IRR), so it cannot run in the T0 local surface. Revisit if/when egress (a hosted API / T2) is built." },
  { slug: "x509-ext", name: "X.509 advanced analysis", family: "PKI", status: "queued", posture: "decode / explain (local)", disposition: "accept", rank: 60, specs: ["RFC 5280"], note: "The local advanced-feature tier of the x509 inspector, now admissible as T0 (D-52: all local compute is free). Deterministic, zero-egress (D-49). Concrete scope (confirmed PRIME 30/06): name-constraint and policy-constraint analysis, SCT / CT parsing, and deeper v3 extension decoding beyond the base inspector; kept distinct from the queued csr-decoder and cert-chain-builder." },
  { slug: "cidr-ext", name: "CIDR advanced planning", family: "Networking & addressing", status: "queued", posture: "calculate / explain (local)", disposition: "accept", rank: 62, specs: ["RFC 4632"], note: "The local advanced-feature tier of the cidr tool, now admissible as T0 (D-52: all local compute is free). Deterministic, zero-egress (D-49). Concrete scope (confirmed PRIME 30/06): VLSM planning (variable-size subnets), supernetting / aggregation of adjacent prefixes, IP-range <-> CIDR, and wildcard masks." },

  // ---- DROPPED (control-defeat / frame-forging — refused on principle) ----
  { slug: "tcp-ao-tfo-recover", name: "TCP-AO / TFO recover", family: "Protocol & packet decoders", status: "dropped", posture: "control defeat", disposition: "drop" },
  { slug: "tcp-rst-injection", name: "TCP RST injection", family: "Protocol & packet decoders", status: "dropped", posture: "injection", disposition: "drop" },
  { slug: "ospf-auth-digest-recover", name: "OSPF auth-digest recover", family: "Protocol & packet decoders", status: "dropped", posture: "auth recover", disposition: "drop" },
  { slug: "pppoe-chap-crack", name: "PPPoE CHAP crack", family: "Protocol & packet decoders", status: "dropped", posture: "credential crack", disposition: "drop" },
  { slug: "ipsec-esp-decrypt-recover", name: "IPsec ESP decrypt / key recover", family: "Protocol & packet decoders", status: "dropped", posture: "decrypt / key recover", disposition: "drop" },
  { slug: "asm-forge-or-recover", name: "ASM forge / recover", family: "Security & WAF", status: "dropped", posture: "forge / recover", disposition: "drop" },
  { slug: "bpdu-superior-root-craft", name: "BPDU superior-root craft", family: "Protocol & packet decoders", status: "dropped", posture: "frame forging", disposition: "drop" },
  { slug: "ospf-lsa-craft-inject", name: "OSPF LSA craft / inject", family: "Protocol & packet decoders", status: "dropped", posture: "forging / injection", disposition: "drop" },
  { slug: "cdp-lldp-edp-spoof-craft", name: "CDP / LLDP / EDP spoof craft", family: "Protocol & packet decoders", status: "dropped", posture: "spoof / forging", disposition: "drop" },
  { slug: "icmp-ndp-redirect-craft", name: "ICMP / NDP redirect craft", family: "Protocol & packet decoders", status: "dropped", posture: "redirect craft", disposition: "drop" },

  // ---- Added 2026-06-29: syslog, API, and cloud-native tools ----------------
  // Syslog: universal for anyone reading FortiGate / BIG-IP / PAN-OS logs.
  { slug: "syslog-pri-decoder", name: "Syslog PRI decoder + encoder", family: "Protocol & packet decoders", status: "live", posture: "decode / encode", disposition: "built", specs: ["RFC 5424", "RFC 3164"], vectors: 17, note: "Decode a PRI value such as 134 into facility (local0) and severity (informational), or encode a facility plus severity back to a PRI and its on-the-wire form. PRI = facility times 8 plus severity. Tiny and universal." },
  { slug: "syslog-message-parser", name: "Syslog message parser", family: "Protocol & packet decoders", status: "queued", posture: "decode / explain", disposition: "accept", rank: 35, specs: ["RFC 5424", "RFC 3164"], note: "Parses a full syslog line (PRI, version, timestamp, hostname, app-name, procid, msgid, structured data, message) for both RFC 5424 and legacy BSD RFC 3164. Reuses the PRI decoder." },
  // API: ranked by value; jwks completes the shipped JWT + OIDC story.
  { slug: "jwks-explainer", name: "JWKS explainer + key matcher", family: "Identity & tokens", status: "live", posture: "decode / explain", disposition: "built", specs: ["RFC 7517", "RFC 7518", "RFC 8037"], vectors: 13, note: "Explains a JSON Web Key Set, flags any private or symmetric key material, and matches a kid against a JWT header. Completes the JWT plus OIDC verification story. Never fetches the jwks_uri." },
  { slug: "cors-preflight-explainer", name: "CORS preflight explainer", family: "HTTP & web", status: "queued", posture: "explain", disposition: "accept", rank: 19, specs: ["Fetch CORS protocol", "RFC 9110"], note: "Paste request and response headers and get a verdict on whether a CORS preflight passes and why. Pairs with secure-headers and the URL inspector." },
  { slug: "webhook-signature-verifier", name: "Webhook signature verifier", family: "Hashing", status: "queued", posture: "verify", disposition: "accept", rank: 51, specs: ["RFC 2104", "HMAC"], note: "Verifies HMAC webhook signatures (GitHub and Stripe style) against a payload and secret. Complements the HMAC tool. Client-side." },
  { slug: "openapi-explainer", name: "OpenAPI / Swagger explainer", family: "HTTP & web", status: "queued", posture: "explain / validate", disposition: "accept", rank: 71, specs: ["OpenAPI 3.1", "OpenAPI 3.0"], note: "Summarizes an OpenAPI document: endpoints, operations, schemas, and security schemes, and flags common issues. Larger and more generic than the other API tools." },
  // Cloud-native: explicitly the end of the queue (lowest priority), new family.
  { slug: "k8s-networkpolicy-explainer", name: "Kubernetes NetworkPolicy explainer", family: "Cloud-native & containers", status: "queued", posture: "explain", disposition: "accept", rank: 152, specs: ["Kubernetes NetworkPolicy"], note: "Explains a NetworkPolicy's ingress and egress rules in plain English and flags allow-all versus default-deny. The most on-brand cloud-native tool, since it is network security." },
  { slug: "k8s-rbac-explainer", name: "Kubernetes RBAC explainer", family: "Cloud-native & containers", status: "queued", posture: "explain", disposition: "accept", rank: 153, specs: ["Kubernetes RBAC"], note: "Parses Role, RoleBinding, and ClusterRole and explains what a binding grants, flagging wildcard verbs or resources and cluster-admin." },
  { slug: "kubeconfig-explainer", name: "kubeconfig explainer", family: "Cloud-native & containers", status: "queued", posture: "decode / explain", disposition: "accept", rank: 154, specs: ["Kubernetes kubeconfig"], note: "Lists the clusters, contexts, and users in a kubeconfig and warns about embedded certificates and tokens. Plays to the privacy-first, zero-egress positioning." },
  { slug: "tfplan-json-explainer", name: "Terraform plan (JSON) explainer", family: "Infrastructure as code (Terraform / Ansible)", status: "queued", posture: "explain", disposition: "accept", rank: 22, specs: ["Terraform JSON plan format"], isNew: true, note: "Takes the JSON from 'terraform show -json plan.out' and summarizes the change set the way you actually need to read it: how many resources are being created, updated, deleted, or replaced, grouped by action, with the replace (destroy-and-recreate) cases called out because those are the dangerous ones. Reading a raw plan is a daily pain and the documented JSON form is stable. Static and local: the plan, which reveals infrastructure shape, never leaves the browser (zero egress, D-49)." },
  { slug: "tfstate-explainer", name: "Terraform state explainer", family: "Infrastructure as code (Terraform / Ansible)", status: "queued", posture: "decode / explain", disposition: "accept", rank: 26, specs: ["Terraform state format (v4)"], isNew: true, note: "Parses a terraform.tfstate file (format version 4) and lays out what it contains: the managed resources and their providers, the module tree, outputs, and the serial and lineage that track the state's identity. Crucially it flags where sensitive values sit in state, which is exactly why you would never paste a state file into a random online viewer. Doing it locally with zero egress (D-49) is the whole point." },
  { slug: "ansible-vault-decryptor", name: "Ansible Vault decryptor", family: "Infrastructure as code (Terraform / Ansible)", status: "queued", posture: "decrypt (deterministic)", disposition: "accept", rank: 40, specs: ["Ansible Vault payload format (AES-256-CTR, PBKDF2-HMAC-SHA256)"], isNew: true, note: "Decrypts an $ANSIBLE_VAULT;1.1;AES256 blob in the browser given the vault password: it reads the embedded salt, derives the key with PBKDF2-HMAC-SHA256, verifies the HMAC-SHA256 integrity tag, then decrypts the AES-256-CTR payload. Deterministic with golden vectors, like the existing JWT and HMAC tools. The privacy case is the strongest on the site: decrypting a secret-bearing vault means the ciphertext AND the password both have to stay on the device (zero egress, D-49), which an online decryptor can never honestly promise. An encrypt direction can follow." },
  { slug: "hcl-to-json", name: "HCL to JSON converter", family: "Infrastructure as code (Terraform / Ansible)", status: "queued", posture: "convert", disposition: "accept", rank: 28, specs: ["HashiCorp HCL2", "Terraform JSON configuration syntax"], isNew: true, note: "Parses HCL2 (the Terraform / Packer / Nomad configuration language) - blocks, labels, attributes, expressions, and heredocs - into the documented Terraform JSON configuration form, so config can be diffed, generated, or fed to tooling that wants JSON. Deterministic and local. Honest scope note: a real HCL2 parser is the bulk of the work here, which is why this sits below the JSON-input tools that only have to read an already-structured file; queued, not trivially built." },

  // ---- Added 2026-06-29: SIEM event formats (logs FortiGate / F5 / PAN ship onward) ----
  { slug: "cef-decoder", name: "CEF decoder (ArcSight)", family: "Logging & SIEM formats", status: "queued", posture: "decode / explain", disposition: "accept", rank: 41, specs: ["ArcSight CEF"], note: "Decodes a Common Event Format line: the pipe-delimited header (version, vendor, product, signature, name, severity) and the key-value extension. Accepted by many SIEMs (Sentinel, Splunk, QRadar) for normalized events." },
  { slug: "splunk-hec-explainer", name: "Splunk HEC event explainer", family: "Logging & SIEM formats", status: "queued", posture: "explain / validate", disposition: "accept", rank: 46, specs: ["Splunk HTTP Event Collector"], note: "Explains and validates a Splunk HTTP Event Collector event: the envelope (time, host, source, sourcetype, index) and the event payload, plus the key-value logging convention." },
  { slug: "leef-decoder", name: "LEEF decoder (QRadar)", family: "Logging & SIEM formats", status: "queued", posture: "decode / explain", disposition: "accept", rank: 56, specs: ["IBM QRadar LEEF"], note: "Decodes an IBM QRadar Log Event Extended Format line: the pipe-delimited header and the delimited attribute list. Narrower than CEF but native to QRadar." },
  { slug: "hsl-log-profile-explainer", name: "F5 HSL and log-profile explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain", disposition: "accept", rank: 80, specs: ["F5 BIG-IP High-Speed Logging"], f5: true, note: "Explains the BIG-IP high-speed logging pipeline (log publisher, log destination, log pool) and the HSL::send iRule pattern, and the payload formats it can carry (syslog, Splunk, ArcSight CEF). F5-specific." },

  // ---- Added 2026-06-29: from the Wireshark f5ethtrailer dissector ----------
  { slug: "f5-eth-trailer-decoder", name: "F5 Ethernet trailer decoder", family: "F5 LTM, iRules & platform", status: "queued", posture: "decode / explain", disposition: "accept", rank: 66, specs: ["F5 Ethernet trailer (f5ethtrailer)"], f5: true, note: "Decodes the F5 Ethernet trailer that BIG-IP appends to packets captured with tcpdump noise flags (:n / :nn / :nnn), magic 0xf5deb0f5 — the Low, Medium, and High details: ingress direction, slot, TMM, VIP name, flow and peer IDs, RST cause, and peer connection info. Diagnostic data only; it deliberately ignores the TLS keylog provider, so it never touches session secrets. Mirrors Wireshark's f5ethtrailer dissector, fully offline." },
  { slug: "f5-rst-cause-explainer", name: "F5 TCP RST cause explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain", disposition: "accept", rank: 77, specs: ["F5 BIG-IP RST cause"], f5: true, note: "Explains the BIG-IP TCP reset cause that the Ethernet trailer and logs report on RST packets as [F5RST: ...] — for example 'No local listener', meaning no virtual server matched the connection — with the usual cause and fix. Gated on the device by TM.rstcause.log and TM.rstcause.pkt. A focused troubleshooting reference." },

  // ---- Added 2026-06-29: Expect (Tcl) scripting helpers --------------------
  { slug: "expect-script-explainer", name: "Expect script explainer", family: "Text & utilities", status: "queued", posture: "explain", disposition: "accept", rank: 7, specs: ["Expect / Tcl"], note: "Breaks down an Expect script — every spawn, expect, send, set timeout, and interact block in plain language — and flags the common pitfalls: hardcoded credentials in a send, a missing or infinite timeout, overly broad expect patterns that fire early, and a missing expect eof. Static analysis only; it never runs the script. Squarely on-brand for network-device automation." },
  { slug: "cron-expression-explainer", name: "Cron expression explainer", family: "Text & utilities", status: "queued", posture: "explain", disposition: "accept", rank: 1, specs: ["POSIX crontab", "Vixie cron extensions"], isNew: true, note: "Reads a crontab expression (the five time fields, plus the common extensions: */step, ranges, lists, names like MON or JAN, and @-shortcuts such as @daily) and says in plain English exactly which minutes, hours, and days it fires on. It describes the matching pattern deterministically rather than computing 'next run from now', so there is no clock in the result and golden vectors stay stable. Flags the classic foot-guns: a step that does not divide evenly, day-of-month and day-of-week both set (an OR, not an AND), and 0 versus 7 for Sunday. One of the most-reached-for CLI utilities anywhere." },
  { slug: "chmod-permission-calculator", name: "chmod permission calculator", family: "Text & utilities", status: "queued", posture: "convert / explain", disposition: "accept", rank: 3, specs: ["POSIX file mode bits", "chmod(1)"], isNew: true, note: "Converts a file mode between octal (755), symbolic (rwxr-xr-x), and a plain-English reading of who can read, write, and execute, in either direction. Covers the special bits most calculators skip: setuid, setgid, and the sticky bit (the leading fourth octal digit) and how they surface in the symbolic form (s, t, and their capital variants when the execute bit is absent). Deterministic and universal; a daily-use utility for anyone on a Unix shell." },
  { slug: "ansi-escape-decoder", name: "ANSI escape decoder", family: "Text & utilities", status: "queued", posture: "decode / explain", disposition: "accept", rank: 44, specs: ["ECMA-48 / ANSI X3.64 SGR + control sequences"], isNew: true, note: "Reads the ANSI escape sequences in a string - the SGR codes for colour and style (bold, dim, italic, underline, the 16/256/true-colour foreground and background forms, reset) plus the common cursor-move and erase sequences - and explains each one in plain language, and can strip them to leave clean text. Deterministic, per ECMA-48. Handy for making sense of captured terminal or log output." },
  { slug: "bash-special-variable-explainer", name: "Bash special-variable explainer", family: "Text & utilities", status: "queued", posture: "explain", disposition: "accept", rank: 47, specs: ["Bash special parameters (Bash manual)"], isNew: true, note: "Explains the special and positional parameters in a shell snippet - $? (last exit status), $$ (PID), $! (last background PID), $#, $0, $-, $_, $IFS, and the positionals $1..$9 - with particular care for the $@ versus $* distinction and why \"$@\" is almost always the correct one for iterating arguments safely. Static explanation only; it never runs anything. Pairs with the cron and chmod utilities." },
  { slug: "expect-pattern-tester", name: "Expect pattern tester", family: "Text & utilities", status: "queued", posture: "test", disposition: "accept", rank: 58, specs: ["Expect / Tcl"], note: "Tests an Expect match pattern against sample device output, honoring Expect's three modes — the default Tcl glob, -re (regex), and -ex (exact) — so you can see why an expect block fires early or hangs. Note that glob * spans newlines, unlike most regex flavours. Offline." },

  // ---- Added 2026-06-29: F5 iControl REST helpers (from clouddocs API set) --
  { slug: "icontrol-rest-path-explainer", name: "iControl REST path explainer", family: "F5 LTM, iRules & platform", status: "queued", posture: "explain", disposition: "accept", rank: 33, specs: ["F5 iControl REST"], f5: true, note: "Parses an iControl REST URL — /mgmt/tm/ltm/pool/~Common~web_pool and the like — and explains each part: the module (ltm, gtm, net, sys, security, apm and the rest), the collection or object, the tilde-encoded ~partition~folder~name path, and the query options ($select, $filter, options, expandSubcollections). Shows the equivalent tmsh object path too. Offline; it never contacts a device." },
  { slug: "icontrol-rest-stats-decoder", name: "iControl REST stats decoder", family: "F5 LTM, iRules & platform", status: "queued", posture: "decode", disposition: "accept", rank: 63, specs: ["F5 iControl REST"], f5: true, note: "Flattens an iControl REST stats response — the deeply nested entries, nestedStats, value, and description wrappers F5 returns — into plain, readable key-value pairs, so a pool or virtual-server stats blob becomes legible at a glance. A pure local JSON transform; nothing leaves the browser." },

  // ---- Added 2026-06-29: F5 Distributed Cloud (XC) helpers (research-backed, pending PRIME ratification) --
  { slug: "f5xc-service-policy-explainer", name: "F5XC service policy explainer", family: "F5 Distributed Cloud (XC)", status: "live", posture: "explain (deterministic)", disposition: "built", vectors: 5, specs: ["ves.io.schema.service_policy"], f5: true, isNew: true, note: "Paste an F5 Distributed Cloud service_policy spec (JSON) and get its match logic spelled out: the server scope, the rule choice, and for a rule_list every rule's action and AND-combined predicates, each matcher rendered with its exact/regex/prefix values, and/or logic, inversion, and the case-sensitivity trap that bites real users. Service policies are 'iRules for XC', the same match-and-act idea. Decode-only, grounded in the official ves.io.schema.service_policy OpenAPI schema; nothing leaves the browser." },
  { slug: "f5xc-http-lb-route-explainer", name: "F5XC HTTP LB route explainer", family: "F5 Distributed Cloud (XC)", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 39, specs: ["ves.io.schema.views.http_loadbalancer"], f5: true, isNew: true, note: "Paste the routes block of an F5XC HTTP load balancer and get the evaluation explained: route type (simple / redirect / direct-response / custom), the path match (prefix vs regex) and HTTP method and header conditions, host-rewrite and header mutations, and the per-route WAF / app-firewall attachment (including inherit-from-LB). Mirrors the per-route WAF and header-routing patterns from XC. Offline decode." },
  { slug: "f5xc-api-path-explainer", name: "F5XC API path & object explainer", family: "F5 Distributed Cloud (XC)", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 81, specs: ["F5 Distributed Cloud API"], f5: true, isNew: true, note: "Decodes an F5XC API path — service_prefix/namespace/{namespace}/{kind}s/{name} — naming the service prefix, namespace, object kind (pluralized by adding 's') and name, and explains the universal metadata / system_metadata / spec object envelope so a raw XC config or API call reads clearly. Niche but handy for XC automation. Offline." },

  // ---- Added 2026-06-29: packet-capture builders (inspired by tcpdump101.com analysis; it covers tcpdump/Fortinet/Check Point/Cisco but NOT F5) --
  { slug: "f5-bigip-tcpdump-builder", name: "BIG-IP tcpdump builder", family: "F5 LTM, iRules & platform", status: "live", posture: "build (deterministic)", disposition: "built", specs: ["tcpdump", "F5 TMM :nnn capture syntax"], vectors: 6, f5: true, isNew: true, note: "Builds a BIG-IP-correct tcpdump command from structured choices — the notoriously confusing TMM interface syntax (0.0:nnn for flow info, the :p/:n peer and noise flags, capturing on a VLAN or self-IP), plus snaplen (-s0), -w to a file, -n/-nn, and a BPF filter. tcpdump101.com builds for Linux, Fortinet, Check Point and Cisco but has no F5 module — this fills that exact gap and leans on the F5 capture expertise. Generates a copyable command; runs entirely client-side." },
  { slug: "bpf-filter-explainer", name: "BPF / tcpdump filter explainer", family: "Protocol & packet decoders", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 52, specs: ["pcap-filter (BPF)"], isNew: true, note: "The decode complement to a capture builder: paste a Berkeley Packet Filter expression (host/net/port, src/dst, tcp/udp/icmp, vlan, tcp[13] flag tests, and/or/not with parentheses) and get a plain-language breakdown of exactly what it matches and the precedence of the logic gates. Where tcpdump101 helps you write a filter, this helps you read one. Offline." },

  // ---- Added 2026-06-29: clean-room capture/flow builders (own engine + UI from official vendor docs/man pages; no code, markup or copy taken from tcpdump101.com — command syntax and flag semantics are facts) --
  { slug: "linux-tcpdump-builder", name: "Linux tcpdump builder", family: "Protocol & packet decoders", status: "queued", posture: "build (deterministic)", disposition: "accept", rank: 38, specs: ["tcpdump(8)", "pcap-filter(7)"], isNew: true, note: "A clean-room tcpdump command builder for plain Linux: interface (incl. any), -n/-nn name resolution, -e link-level, -X/-XX payload, -s snaplen, -c count, -w/-C/-G/-W file output and rotation, -v verbosity, and a BPF filter assembled from L2-L4 primitives. Built from the tcpdump man page and our own UI — no code or wording taken from tcpdump101.com. Generates a copyable command; fully client-side." },
  { slug: "fortios-sniffer-builder", name: "FortiOS packet sniffer builder", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "build (deterministic)", disposition: "accept", rank: 45, specs: ["FortiOS diagnose sniffer packet"], fortinet: true, isNew: true, note: "Builds a FortiGate 'diagnose sniffer packet <interface> <filter> <verbosity> <count> <timestamp-format>' command: the interface (incl. any), the FortiOS filter expression (host/net/port/proto with and/or), the verbosity level 1-6 (and what each adds — headers, payload, interface names), packet count, and the a/l timestamp format. Clean-room from the FortiOS CLI reference; our own engine. Offline." },
  { slug: "fortios-flow-debug-builder", name: "FortiOS debug flow builder", family: "Fortinet FortiGate & FortiOS", status: "queued", posture: "build (deterministic)", disposition: "accept", rank: 54, specs: ["FortiOS diagnose debug flow"], fortinet: true, isNew: true, note: "Assembles the multi-line 'diagnose debug flow' sequence used to trace how FortiGate processes a session — filter (addr/port/proto), 'diagnose debug flow show function-name enable', trace start with a packet count, and 'diagnose debug enable' — with the matching cleanup commands. Maps the notoriously order-sensitive debug-flow recipe. Clean-room from FortiOS docs; offline." },

  // ---- Added 2026-06-29: F5OS platform (VELOS F5OS-C / rSeries F5OS-A) helpers, research-backed --
  { slug: "f5os-restconf-path-explainer", name: "F5OS RESTCONF path explainer", family: "F5OS platform (VELOS / rSeries)", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 18, specs: ["F5OS RESTCONF", "OpenConfig YANG"], f5: true, isNew: true, note: "Decodes an F5OS API path — /restconf/data/openconfig-system:system/aaa, /restconf/data/f5-tenants:tenants/tenant=tenant1/config/running-state and the like — naming the YANG module prefix (openconfig-system, openconfig-interfaces, openconfig-vlan, f5-tenants, f5-system-partition, f5-system-slot, f5-tenant-images), the container hierarchy, the module:node prefixing convention, and list keys (tenant=tenant1). Also explains the :8888/restconf/data vs :443/api/data duality (F5OS 1.8+) and the X-Auth-Token (itself a JWT). Parallels the iControl REST path explainer for the F5OS layer. Offline." },
  { slug: "f5os-tenant-config-explainer", name: "F5OS tenant config explainer", family: "F5OS platform (VELOS / rSeries)", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 75, specs: ["F5OS f5-tenants YANG"], f5: true, isNew: true, note: "Paste an f5-tenants:tenants config block and get it read back plainly: the running-state lifecycle (Configured -> Provisioned -> Deployed), image, allowed slots/nodes (VELOS blades), vCPU and memory allocation, mgmt IP, assigned VLANs, and crypto/appliance-mode flags — plus the VELOS-vs-rSeries differences (chassis partitions and slots exist only on F5OS-C). Offline decode." },

  // ---- Added 2026-06-29: certificate lifecycle (CLM-adjacent; CertLedger Pro space) --
  // (#4 SCT decode folds into x509; #6 PQC named-groups fold into cipher — enhancements, not new entries. Ranks provisional, pending PRIME's D-56 ordering.)
  { slug: "cert-renewal-planner", name: "Certificate renewal planner", family: "PKI", status: "live", posture: "calculate / explain", disposition: "built", vectors: 5, specs: ["CA/Browser Forum SC-081v3", "RFC 5280", "RFC 8555"], isNew: true, note: "Enter a cert's notBefore/notAfter for its validity length, whether that length is within the SC-081v3 cap for its issuance date (398 -> 200 -> 100 -> 47 days), the renewals per year it implies and the escalation at each future cap, the DCV/SII validation-reuse windows for that era, and a recommended renew-by date. Pure date math; the only clock-relative line (days remaining) is client-side. Public TLS only; private PKI noted out of scope. First of the CLM-adjacent set inspired by the CertLedger Pro space." },
  { slug: "csr-decoder", name: "CSR decoder", family: "PKI", status: "live", posture: "decode / verify", disposition: "built", specs: ["PKCS#10 / RFC 2986", "RFC 5280"], vectors: true, note: "Decodes a PEM PKCS#10 signing request: subject DN, SANs, key type/size, signature algorithm, requested v3 extensions, and verifies the CSR self-signature. The request-side sibling to the x509 inspector. Offline." },
  { slug: "cert-chain-builder", name: "Certificate chain builder & validator", family: "PKI", status: "queued", posture: "order / validate (offline)", disposition: "accept", rank: 4, specs: ["RFC 5280"], isNew: true, note: "Paste a leaf plus intermediates (any order); builds the chain by AKI -> SKI, flags missing or extra intermediates, checks validity-window overlap, basicConstraints/pathLenConstraint, keyUsage, and name chaining. Explains revocation checks rather than performing them (zero egress). Complements x509 (single cert) by working the whole path." },
  { slug: "cert-key-match", name: "Key / cert / CSR match + SPKI fingerprint", family: "PKI", status: "queued", posture: "compare / fingerprint", disposition: "accept", rank: 65, specs: ["RFC 5280", "RFC 7469"], isNew: true, note: "Confirms a public key matches a certificate or CSR (modulus / curve-point compare) and computes the DER and SubjectPublicKeyInfo SHA-256 fingerprints (for pinning, CT, and key matching). Pairs with x509, the CSR decoder, and hash. Offline." },
  { slug: "ocsp-crl-decoder", name: "OCSP / CRL decoder", family: "PKI", status: "queued", posture: "decode / explain", disposition: "accept", rank: 61, specs: ["RFC 6960", "RFC 5280"], isNew: true, note: "Decodes an OCSP response (status, thisUpdate/nextUpdate, producedAt, signature) or a CRL (issuer, this/next update, revoked serials + reason codes). x509 reads a cert's revocation POINTERS; this decodes what is actually at them. Offline; never fetches." },
  { slug: "acme-flow-explainer", name: "ACME flow explainer", family: "PKI", status: "queued", posture: "explain (deterministic)", disposition: "accept", rank: 72, specs: ["RFC 8555", "RFC 9773"], isNew: true, note: "Walks the ACME state machine (directory -> account -> order -> authorization -> HTTP-01 / DNS-01 / TLS-ALPN-01 challenge -> finalize -> certificate) and the ARI renewal-info extension, in the style of the F5/Fortinet explainers. The automation the 47-day schedule makes mandatory. Static; no network." },
  { slug: "color-contrast-format", name: "Color contrast & format", family: "Web front-end & CSS", status: "queued", posture: "compute / convert", disposition: "accept", rank: 9, specs: ["WCAG 2.1", "CSS Color 4"], isNew: true, note: "Enter two colors and get the WCAG contrast ratio with AA/AAA pass or fail for normal and large text, plus conversion across hex, rgb, hsl, and oklch. Pure math, zero egress. The exact utility that would have front-run the D-71 severity-token work." },
  { slug: "css-specificity-calculator", name: "CSS specificity calculator", family: "Web front-end & CSS", status: "queued", posture: "compute / explain", disposition: "accept", rank: 10, specs: ["CSS Selectors 4", "CSS Cascade 5"], isNew: true, note: "Computes selector specificity (id, class/attr/pseudo-class, element) for any selector, the single thing every front-end dev gets wrong under pressure. Can broaden to shorthand expansion and cascade/at-rule listing. Compute, never guess, taken literally." },
  { slug: "html-structure-explainer", name: "HTML structure & DOM explainer", family: "Web front-end & CSS", status: "queued", posture: "parse / explain", disposition: "accept", rank: 11, specs: ["HTML Living Standard", "WCAG 2.1"], isNew: true, note: "Paste HTML and get the parsed element tree (the DOM), attributes, the HTML5 parser error recovery for unclosed or misnested tags, void and deprecated elements, and quick a11y flags (missing alt, missing lang, heading-order jumps). Parses inertly (DOMParser into a detached document, no script execution, no render). Distinct from xml-decoder (HTML5 rules are not XML)." },
  { slug: "css-selector-tester", name: "CSS selector tester", family: "Web front-end & CSS", status: "queued", posture: "match / explain", disposition: "accept", rank: 13, specs: ["CSS Selectors 4"], isNew: true, note: "Inert: parse pasted HTML, run a CSS (or XPath) selector against the detached document, and show which nodes match plus the selector specificity. Pairs with the HTML explainer and specificity calculator; may co-build with them as one HTML + selectors surface." },
  { slug: "meta-og-jsonld-inspector", name: "Meta, Open Graph & JSON-LD inspector", family: "HTTP & web", status: "queued", posture: "parse / explain", disposition: "accept", rank: 14, specs: ["HTML Living Standard", "Open Graph protocol", "schema.org"], isNew: true, note: "Decodes a page head: title, description, canonical, Open Graph and Twitter cards, and schema.org JSON-LD. Useful for web and SEO work. Likely a mode of the HTML structure explainer rather than a standalone tool." },
  { slug: "traceroute-output-explainer", name: "traceroute / tracepath output explainer", family: "Networking & addressing", status: "queued", posture: "decode / explain", disposition: "accept", rank: 37, specs: ["RFC 792", "RFC 1393"], isNew: true, note: "Decodes traceroute, tracepath, or mtr output: each hop and its probe RTTs, and flags the standard misreadings (three asterisks means filtered or no-reply, not down here; a latency spike at one hop does not implicate that hop; ECMP produces varying paths) plus tracepath PMTU discovery. Companion to the dig and nslookup explainers. Runs nothing (zero egress)." },
];

// ---- Consolidation decisions (for the console) -----------------------------
export interface MergeDecision {
  id: string;
  outcome: "merged" | "separate";
  text: string;
}
export const MERGES: MergeDecision[] = [
  { id: "M1", outcome: "merged", text: "vlsm-supernet → cidr (cidr becomes full subnetting)." },
  { id: "M2", outcome: "merged", text: "tunneling-overhead-reference → mtu-mss." },
  { id: "M4", outcome: "merged", text: "asn + route-object-prefix → asn-prefix." },
  { id: "M5", outcome: "merged", text: "hex-url → base64 (unified codec)." },
  { id: "M3", outcome: "separate", text: "layers-nest-explainer kept separate from layered-packet-decoder." },
  { id: "M6", outcome: "separate", text: "xml-decoder kept separate from saml-decoder (different audiences)." },
  { id: "M7", outcome: "separate", text: "bits-bytes kept separate from mtu-mss (throughput vs bytes-on-wire)." },
  { id: "M8", outcome: "merged", text: "curl-command-explainer → http-request-translator (one paste: explain + translate; the parse is shared)." },
];

// ---- Helpers ---------------------------------------------------------------
export const byStatus = (s: CatalogueStatus) => CATALOGUE.filter((t) => t.status === s);
export const byFamily = (f: string) => CATALOGUE.filter((t) => t.family === f);
export const counts = () => ({
  live: byStatus("live").length,
  queued: byStatus("queued").length,
  deferred: byStatus("deferred").length,
  dropped: byStatus("dropped").length,
  total: CATALOGUE.length,
  f5Queued: CATALOGUE.filter((t) => t.status === "queued" && t.f5).length,
  fortinetQueued: CATALOGUE.filter((t) => t.status === "queued" && t.fortinet).length,
  netskopeQueued: CATALOGUE.filter((t) => t.status === "queued" && t.netskope).length,
  extremeQueued: CATALOGUE.filter((t) => t.status === "queued" && t.extreme).length,
  udp: CATALOGUE.filter((t) => t.isNew).length,
});
/** LIVE tools with a ratified-but-unbuilt merge — build BEFORE any new queue tool. */
export const consolidationBacklog = () => CATALOGUE.filter((t) => t.consolidationPending);
/**
 * buildQueue — the Phase-1 build order: every accepted (not-yet-built) tool,
 * sorted by its global value rank (1 = build first). PRIME-ratified 2026-06-28,
 * this single highest-to-lowest ranking lives on each entry's `rank` field and
 * is the durable source of truth, superseding the prior per-family VALUE_RANK
 * scheme. Any accepted tool missing a rank sorts last, alphabetically.
 */
export const buildQueue = () =>
  CATALOGUE.filter((t) => t.disposition === "accept").sort(
    (a, b) =>
      (a.rank ?? Number.MAX_SAFE_INTEGER) - (b.rank ?? Number.MAX_SAFE_INTEGER) ||
      a.name.localeCompare(b.name),
  );

